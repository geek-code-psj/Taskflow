# ============================================================
# apps/web — Environment Variables
# ============================================================

# In production on Railway, leave this empty —
# Vite proxy config routes /api → the API service via Railway internal networking.
# For local dev, set to your API URL:
VITE_API_URL=http://localhost:4000
